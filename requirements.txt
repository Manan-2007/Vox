# Vox — Python 3.12 (macOS arm64 verified)
# Pinned, mutually compatible set for the Milestone 0 spike.

# --- CV / landmarks ---
mediapipe==0.10.35
opencv-python==4.14.0.94
# mediapipe depends on opencv-contrib-python. Both packages install the same
# `cv2` module, so they are pinned to the SAME upstream OpenCV version to keep
# cv2 consistent no matter which one pip writes last.
opencv-contrib-python==4.14.0.94

# --- ML ---
tensorflow==2.20.0
numpy==2.5.1
scikit-learn==1.9.0

# --- Backend ---
fastapi==0.141.0
uvicorn==0.52.0
websockets==16.1.1
