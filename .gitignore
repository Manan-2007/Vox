# Python
venv/
__pycache__/
*.py[cod]

# Node
node_modules/
frontend/dist/

# ML artifacts. Raw extraction cache is regenerated by ml/extract.py.
ml/data/

# Source dictionary clips: ~88 MB of ISLRTC video. Not shipped in the repo —
# the signers' likeness belongs to them, and the landmark motion under
# frontend/public/signs/ (a twentieth of the size) is what the app actually
# consumes. Re-downloadable with `python ml/fetch_dictionary.py --download`.
ml/videos/

# OS / editor
.DS_Store
.vscode/
.idea/
